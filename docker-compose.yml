version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=app.py
    volumes:
      - ./app.py:/app/app.py
      - ./templates:/app/templates
    restart: unless-stopped
